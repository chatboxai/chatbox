# Current State

For agents, not humans. What's true right now. Edit in place after every task (see `AGENT_RULES.md` End-of-Task Checklist items 3-4) — when an item is fully resolved, delete it rather than leaving it as a historical note. History lives in `git log` and `CHANGELOG.md`, not here.

## Active Cycle

- Development version is `1.0.2-beta` (root and `release/app` manifests). Keep working under this version until the user explicitly says it's released, then move to `1.0.3-beta`.
- Last installed/verified local macOS build: `1.0.2-beta` (`CFBundleShortVersionString` confirmed, codesign valid, ad-hoc signed for local testing).

## In Flight / Next Up

- QA gates: no skipped tests, all production TypeScript measured, ~1,390 unit + 37 integration + 10 Electron E2E scenarios, repo-wide biome ratchet (`qa:biome-ratchet`, baseline 0 errors / 682 warnings — reduce opportunistically and ratchet down). Coverage floor **25% statements/lines, 18% branches/functions** (actuals ~28/21/22/28.5 after the 2026-07-08 §6.5 cycle: SAR+KB IPC handlers, KB db, renderer session crud/messages, CommandPalette smokes — all real-libsql or in-memory-fake harnesses, patterns in ARCHITECTURE_NOTES). Remaining known-untested high-value targets: `MessageList.tsx`, `InputBox.tsx` render sections (jsdom+RTL infra proven — `CommandPalette.test.tsx` shows the Mantine wrapper + ResizeObserver/matchMedia stubs + findByText-for-portal-transitions pattern), `stores/session/forks.ts` (433 lines, branch-tree logic).
- FABLE_REVIEW.md closeout: **everything except the last two phases is done** (see FABLE_REVIEW §0): all P0/P1/P2, §6.2+§7.6, SEC-6 (mobile/web dropped), F2 (⌘K palette), F1+§9.4 (onboarding/empty state), F4 (FTS5 chat search) — all shipped 2026-07-07. Remaining from the user-approved plan: **phase E** (electron-builder 26.15.6 upgrade + drop `patches/app-builder-lib@26.8.1.patch` + Electron 42.5.0→42.5.2 — NOT 43.0.0, unsettled) and **phase F** (§6.5 coverage cycle). A11y note for new UI: icon-only buttons must get `aria-label` (pattern: `SessionList.tsx`); don't wrap hover-triggered `Menu.Target` buttons in `Tooltip` — the dropdown races it. Tool-error convention for new toolsets: infrastructure failures throw; see ARCHITECTURE_NOTES. Floating-promise convention: intentional fire-and-forget calls are marked `void fn()`.
- **Post-mobile-drop residual cleanups (opportunistic, behavior-neutral):** ~25 always-true `platform.type === 'desktop'` guards remain (PlatformType is now just `'desktop'`, so tsc flags any non-desktop comparison; the guards are dead but harmless — unwrap when touching those files). `--mobile-safe-area-inset-*` CSS vars in `index.css`/`globals.css` + a few components resolve to 0px on desktop — strip when touching. Per-provider `useProxy` setting (`$providerId.tsx`, `RequestOptions`) is a dead mobile-CORS-proxy plumb-through — remove UI+field in a provider-settings pass. `resources/splash*.png`/`icon-background/foreground.png` were capacitor-assets sources; `scripts/regen-app-icons.mjs` still mentions mobile outputs — trim on next icon regen.
- **SEC-2 follow-up:** Routine Electron 42→43 bump once 43.x has patch releases older than pnpm's 7-day `minimumReleaseAge` and has settled (43.0.0 shipped 2026-06-30 with no patches yet as of 07-07; 42 support ends 2026-10-20). Currently on Electron 42.5.1 + electron-builder 26.15.6 with the rebased collector patch (`patches/app-builder-lib@26.15.6.patch` — NOT droppable, see ARCHITECTURE_NOTES; upstream's pnpm-collector fix collects the wrong tree for this repo).
- **P3 chat-surface follow-ups.** The P3 phase shipped 2026-07-07; `Sidebar.tsx` + `ThreadHistoryDrawer.tsx` migrated off MUI `SwipeableDrawer` to Mantine `Drawer`/fixed aside in the mobile-drop phase (2026-07-07). Remaining: (1) **features F1 (onboarding) / F2 (⌘K palette) / F4 (chat full-text search)** — phases B–D of the current plan; §9.4 (empty state points at provider setup) folds into F1/phase C. (2) `InputBox.tsx` is ~1,758 lines with all pure decision logic extracted+tested; further shrink = extracting stateful render sections, only worth it with component-level tests (§6.5 target).

## Open Product Questions

- Static landing site at `landing/`. Deployment is the user's responsibility (own infrastructure). Local container: `workspaice-landing`, port 8080, image `workspaice-landing:latest`.
