# Current State

For agents, not humans. What's true right now. Edit in place after every task (see `AGENT_RULES.md` End-of-Task Checklist items 3-4) — when an item is fully resolved, delete it rather than leaving it as a historical note. History lives in `git log` and `CHANGELOG.md`, not here.

## Active Cycle

- Development version is `1.0.2-beta` (root and `release/app` manifests). Keep working under this version until the user explicitly says it's released, then move to `1.0.3-beta`.
- Last installed/verified local macOS build: `1.0.2-beta` (`CFBundleShortVersionString` confirmed, codesign valid, ad-hoc signed for local testing).

## In Flight / Next Up

- QA expansion is active. Deterministic gates now enforce no skipped tests, measure all production TypeScript, run 1,266 unit + 37 integration + 7 Electron E2E scenarios, and ratchet repo-wide biome diagnostics (`qa:biome-ratchet`, baseline 0 errors / 769 warnings — reduce opportunistically and ratchet down). Current coverage floor is 20% statements/lines and 15% branches/functions. Covered this cycle: `src/main/store-node.ts` (0→~83%; `store-node.test.ts` — see ARCHITECTURE_NOTES for the electron-store/conf mocking traps), `src/main/session-attachment-rag/chunking.ts` (0→~94%; structural-invariant assertions that don't over-specify MDocument's recursive splitting), and `src/main/session-attachment-rag/db.ts` (0→~87% lines/92% funcs/85% branches; `db.test.ts` drives real libsql against a temp file via the `SESSION_ATTACHMENT_RAG_DB_PATH`/`..._VECTOR_DB_PATH` env overrides + `resetModules`, mocking only the `@mastra/libsql` vector store — see ARCHITECTURE_NOTES). Next highest-risk coverage targets: the remaining RAG DB/IPC main paths (`knowledge-base/db.ts`, both `session-attachment-rag/ipc-handlers.ts` + `knowledge-base/ipc-handlers.ts` — heavier, libsql-backed), then `InputBox.tsx`, `MessageList.tsx`, and session CRUD/messages.
- FABLE_REVIEW.md: **all P0/P1/P2 items, the low/opportunistic tier, AND §6.2 + §7.6 are done** (see FABLE_REVIEW §0 for the ✅ list). Remaining: P3 redesign-coupled items, the SEC-6 mobile decision, and §6.5 coverage on high-risk targets (active QA cycle). A11y note for new UI: icon-only buttons must get `aria-label` (pattern: `SessionList.tsx`); don't wrap hover-triggered `Menu.Target` buttons in `Tooltip` — the dropdown races it. Tool-error convention for new toolsets: infrastructure failures throw; see ARCHITECTURE_NOTES. Floating-promise convention: intentional fire-and-forget calls are marked `void fn()` — keep new code compliant (`lint/nursery/noFloatingPromises` is clean repo-wide).
- **SEC-2 follow-ups:** (1) Routine Electron 42→43 bump once 43.x has minors older than pnpm's 7-day `minimumReleaseAge` and has settled (42 support ends 2026-10-20). (2) Evaluate upgrading electron-builder (≥26.13 has an upstream pnpm-collector dedup fix, PR #9618) and dropping `patches/app-builder-lib@26.8.1.patch` — own task, needs the full `qa:release:*` + asar-completeness verification described in `ARCHITECTURE_NOTES.md`. (`qa:release:win` smoke is now verified working — NSIS Setup.exe builds, asar complete, node-fetch@3.3.2 only.)
- **P3 chat-surface redesign — plan agreed, not started.** Sequence (each an independently shippable commit): (1) characterization tests for `InputBox.tsx`/`MessageList.tsx` as they behave today — doubles as the §6.5 coverage remainder and the safety net for the split; (2) split `InputBox.tsx` (2,064 lines) by concern — attachments / slash-skills / model selector / send pipeline — pure structural refactor, tests stay green; (3) visual redesign of `MessageList`/`Message`/composer against design tokens, folding in the cheap UX wins (composer first-use hint §9.3, disclaimer placement §9.5, provider capability-icon legend §9.7); (4) MUI→Mantine migration of only the leaves touched in (3) — inventory `grep -rl "@mui/" src/renderer`, no standalone rewrite; (5) settings three-pane→stacked under ~700px (§9.2, independent, slot anywhere); (6) features on the same surfaces — F1 first-run onboarding (§9.4), F2 ⌘K palette (`@mantine/spotlight` already a dep), F4 full-text chat search (libsql FTS5). Rationale: characterize→split→restyle keeps the untested 2kloc composer safe; alternative (restyle-in-place first) is faster but higher-regression. Open before starting: how far the visual change goes (token refresh vs. structural rethink — decides whether the (2) boundaries survive), whether F1/F2/F4 belong in this phase, and the SEC-6/mobile decision (narrows responsive work if mobile is dropped).

## Open Product Questions

- Should mobile and web build paths be removed now or only deprioritized while desktop cleanup happens first?
- Static landing site at `landing/`. Deployment is the user's responsibility (own infrastructure). Local container: `workspaice-landing`, port 8080, image `workspaice-landing:latest`.
- Mobile SQLite encryption is tracked as a known limitation — see `ARCHITECTURE_NOTES.md` Known Risk Areas for details, not duplicated here.
